{
  "name": "webrtc-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite & node -e \"setTimeout(() => require('./scripts/showNetworkUrls')(3000), 1000)\"",
    "local": "vite --host",
    "build": "vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.0.5"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "firebase": "^8.2.10"
  }
}
